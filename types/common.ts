export type LinkItem = {
  label: string;
  href: string;
};

export type Metric = {
  value: string;
  label: string;
};

export type SocialLink = {
  platform: string;
  label: string;
  url: string;
};
